print vec[first]
print (vec[first])
vec <- c(first=1, second=3)
print (vec[first])
install.packages("rl")
print(vec['first'])
source('~/practice.R', echo=TRUE)
vec <- c(1,2,3)
codes <- c(first, second, third)
names(vec) = codes
print(vec["second"])
vec <- c(1,2,3)
codes <- c("first", "second", "third")
names(vec) = codes
print(vec["second"])
setwd("C:/Users/admin/Desktop/Research/GitHub Repo/MTBS NOAA project")
firedata <- read.csv("mtbs_data.csv")
listOfStns <- firedata$closestStnID
listOfStns <- listOfStns[listOfStns != '0']
listOfStns <- unique(listOfStns)
#download noaa data
for(i in 1:length(listOfStns)){
if(!file.exists(paste0('noaa/noaaout/', listOfStns[i], ".csv"))){
outfile <- paste0('noaa/noaaout/', listOfStns[i], ".csv")
df<-ghcnd(listOfStns[i], refresh=TRUE)
write.csv(df, outfile)
print("written file")
}
print(i)
}
library(rnoaa)
listOfStns <- firedata$closestStnID
listOfStns <- listOfStns[listOfStns != '0']
listOfStns <- unique(listOfStns)
#download noaa data
for(i in 1:length(listOfStns)){
if(!file.exists(paste0('noaa/noaaout/', listOfStns[i], ".csv"))){
outfile <- paste0('noaa/noaaout/', listOfStns[i], ".csv")
df<-ghcnd(listOfStns[i], refresh=TRUE)
write.csv(df, outfile)
print("written file")
}
print(i)
}
